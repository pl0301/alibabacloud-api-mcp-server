[project]
name = "alibabacloud.mcp-proxy"
version = "0.2.14"
description = "Local stdio MCP proxy for Alibaba Cloud OpenAPI MCP servers."
readme = "README.md"
license = "Apache-2.0"
requires-python = ">=3.10"
authors = [
    { name = "Alibaba Cloud" },
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: Apache Software License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Software Development :: Libraries",
]
dependencies = [
    "aiohttp>=3.9.0",
    # credentials-python Windows OAuth config.json overwrite fix (os.replace).
    "alibabacloud-credentials>=1.0.10",
    "alibabacloud-openapi-util>=0.2.4",
    "alibabacloud-tea-openapi>=0.4.4",
    "alibabacloud-tea-util>=0.3.14",
    "httpx[socks]>=0.28.1",
    "mcp>=1.27.0,<2.0.0",
]

[project.urls]
Homepage = "https://github.com/aliyun/alibabacloud-api-mcp-server"
Repository = "https://github.com/aliyun/alibabacloud-api-mcp-server"

[project.scripts]
"alibabacloud.mcp-proxy" = "alibabacloud.mcp_proxy.cli:main"

# Compatibility shim: uv 0.8.4 on Windows incorrectly resolves
# alibabacloud.mcp-proxy as alibabacloud.exe.
# Do not remove until support for affected uv versions is dropped.
"alibabacloud" = "alibabacloud.mcp_proxy.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/alibabacloud"]

[tool.pytest.ini_options]
asyncio_mode = "auto"

[dependency-groups]
dev = [
    "pytest>=9.0.3",
    "pytest-asyncio>=1.3.0",
    "pytest-aiohttp>=1.0.0",
]
